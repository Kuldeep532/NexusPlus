# Gambling domain dataset

This directory is reserved for the user-supplied StevenBlack `gambling` hosts snapshot.

Upload the raw hosts file as:

`android/app/src/main/cpp/data/stevenblack/gambling-only/hosts`

The build pipeline will validate and compile it into the protected native lookup representation.
