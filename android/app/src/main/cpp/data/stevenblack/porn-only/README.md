# Porn domain dataset

Upload the StevenBlack `porn` extension raw hosts snapshot as:

`android/app/src/main/cpp/data/stevenblack/porn-only/hosts`

Do not upload a `Unified hosts + porn` variant here; Nexus combines category inputs during preprocessing.
