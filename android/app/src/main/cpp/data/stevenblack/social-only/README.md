# Social-media domain dataset

Upload the StevenBlack `social` extension raw hosts snapshot as:

`android/app/src/main/cpp/data/stevenblack/social-only/hosts`

Do not upload a `Unified hosts + social` variant here; Nexus combines category inputs during preprocessing.
