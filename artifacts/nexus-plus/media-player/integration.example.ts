import { NexusMediaPlayer } from './NexusMediaPlayer';

// Register this component from your mobile app's navigation layer.
// Example:
// <Stack.Screen name="Media" component={() => <NexusMediaPlayer onBack={() => navigation.goBack()} />} />;

export default NexusMediaPlayer;
