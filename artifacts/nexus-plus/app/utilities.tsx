import { Feather } from '@expo/vector-icons';
import { Pressable, ScrollView, StyleSheet, Text, View } from 'react-native';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { useColors } from '@/hooks/useColors';
const tools = [{ icon: 'camera' as const, title: 'OCR text extractor', desc: 'Turn scanned pages and images into readable text.', accent: '#4AA7F5' }, { icon: 'file' as const, title: 'PDF ⇄ image converter', desc: 'Convert documents locally, one page or a whole file.', accent: '#55E6C1' }, { icon: 'scissors' as const, title: 'Audio trimmer & joiner', desc: 'Clip or merge voice notes and TTS exports.', accent: '#5B5DE6' }, { icon: 'filter' as const, title: 'Speech sanitizer', desc: 'Clean URLs, symbols, and clutter before listening.', accent: '#F0B35B' }];
export default function UtilitiesScreen() { const colors = useColors(); const insets = useSafeAreaInsets(); return <ScrollView style={{ backgroundColor: colors.background }} contentContainerStyle={{ paddingTop: insets.top + 18, paddingBottom: insets.bottom + 30 }}><View style={styles.heading}><Text style={[styles.kicker, { color: colors.primary }]}>SMART UTILITIES</Text><Text style={[styles.title, { color: colors.foreground }]}>Make more readable.</Text><Text style={[styles.sub, { color: colors.mutedForeground }]}>Practical tools for the moments generic phone apps miss.</Text></View>{tools.map((tool) => <Pressable key={tool.title} accessibilityRole="button" accessibilityLabel={`${tool.title}. ${tool.desc}`} onPress={() => {}} style={({ pressed }) => [styles.tool, { backgroundColor: colors.card, borderColor: colors.border }, pressed && { opacity: 0.76 }]}><View style={[styles.toolIcon, { backgroundColor: `${tool.accent}20` }]}><Feather name={tool.icon} size={22} color={tool.accent} /></View><View style={styles.copy}><Text style={[styles.toolTitle, { color: colors.foreground }]}>{tool.title}</Text><Text style={[styles.toolDesc, { color: colors.mutedForeground }]}>{tool.desc}</Text></View><Feather name="arrow-up-right" size={18} color={colors.mutedForeground} /></Pressable>)}<View style={[styles.footer, { backgroundColor: colors.secondary }]}><Feather name="lock" size={17} color={colors.primary} /><Text style={[styles.footerText, { color: colors.secondaryForeground }]}>All processing happens on your device. No uploads, no accounts, no surprises.</Text></View></ScrollView>; }
const styles = StyleSheet.create({ heading: { paddingHorizontal: 20, marginBottom: 28 }, kicker: { fontSize: 11, letterSpacing: 1.8, fontFamily: 'Inter_700Bold', marginBottom: 10 }, title: { fontSize: 30, fontFamily: 'Inter_700Bold', marginBottom: 7 }, sub: { fontSize: 14, lineHeight: 20, fontFamily: 'Inter_400Regular' }, tool: { marginHorizontal: 20, minHeight: 88, borderRadius: 18, borderWidth: 1, padding: 14, flexDirection: 'row', alignItems: 'center', marginBottom: 11 }, toolIcon: { width: 47, height: 47, borderRadius: 15, alignItems: 'center', justifyContent: 'center' }, copy: { flex: 1, marginLeft: 13, marginRight: 8 }, toolTitle: { fontSize: 14, fontFamily: 'Inter_600SemiBold', marginBottom: 5 }, toolDesc: { fontSize: 12, lineHeight: 17, fontFamily: 'Inter_400Regular' }, footer: { margin: 20, borderRadius: 16, padding: 15, flexDirection: 'row', gap: 10, alignItems: 'center' }, footerText: { flex: 1, fontSize: 12, lineHeight: 18, fontFamily: 'Inter_500Medium' } });