Time Assisted bundled audio assets.

Required file:
- time_assisted_beep.mp3 — short, neutral beep played immediately before time/date announcements.

The application references this exact path so the filename must remain stable.
