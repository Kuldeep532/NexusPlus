name: Android APK (Retired)

# Retired intentionally. This workflow has no trigger and cannot run.
# Use `.github/workflows/manual-android-apk.yml` only when a manual APK build is required.
on: {}

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired workflow. Use the dedicated manual workflow.'
