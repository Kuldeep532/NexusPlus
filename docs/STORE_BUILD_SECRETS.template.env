# Android build-time configuration
#
# Add this ONLY to the GitHub Actions production environment secret:
# NEXUS_API_BASE_URL
#
# Supabase-only testing gateway:
# https://cpbwiarqlvtlnwbkmpws.supabase.co/functions/v1/nexus-api-gateway
#
# Do not display or copy this value into app UI.
# Provider API keys and other sensitive credentials stay server-side in
# Supabase Edge Function secrets and never go into EXPO_PUBLIC_* variables.
NEXUS_API_BASE_URL=
